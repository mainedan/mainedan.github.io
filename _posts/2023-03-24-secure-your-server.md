# Securing a Linux server
After creating a new Linux server, in my example it's debian/Ubuntu, first install all updates

If logged in ar root, don't use the sudo command 
```
sudo apt update && sudo apt upgrade -y
```
